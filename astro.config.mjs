import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://susmitaraut75.github.io',
  // base: '/repo-name', // Uncomment and set if deploying to username.github.io/repo-name
  output: 'static',
});
